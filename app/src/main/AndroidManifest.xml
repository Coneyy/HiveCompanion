<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="coneys.com.github.hivecompanion">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screens.matchHistory.view.HistoryActivity" />
        <activity android:name="coneys.com.github.hivecompanion.screens.start.login.LoginActivity">

        </activity>
        <activity android:name=".screens.intro.IntroActivity"
            android:theme="@style/NoActionBar"/>
        <activity android:name="coneys.com.github.hivecompanion.screens.start.register.RegisterActivity" />
        <activity
            android:name=".screens.splash.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".screens.scoreTable.ScoreActivity" />
    </application>

</manifest>